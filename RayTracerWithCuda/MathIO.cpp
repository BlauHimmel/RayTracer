#include "stdafx.h"

#include "Vector2f.h"
#include "Vector3f.h"
#include "Vector4f.h"
#include "Matrix4f.h"

namespace EasyMath
{
/*
	std::ostream& operator << (std::ostream& Out, const Vector2f& Vector)
	{
		Out << "Vector2f <" << Vector.X() << ", " << Vector.Y() << ">";
		return Out;
	}

	std::istream& operator >> (std::istream& In, Vector2f& Vector)
	{
		In >> Vector.X() >> Vector.Y();
		return In;
	}

	std::ostream& operator << (std::ostream& Out, const Vector3f& Vector)
	{
		Out << "Vector3f <" << Vector.X() << ", " << Vector.Y() << ", " << Vector.Z() << ">";
		return Out;
	}

	std::istream& operator >> (std::istream& In, Vector3f& Vector)
	{
		In >> Vector.X() >> Vector.Y() >> Vector.Z();
		return In;
	}

	std::ostream& operator << (std::ostream& Out, const Vector4f& Vector)
	{
		Out << "Vector4f <" << Vector.X() << ", " << Vector.Y() << ", " << Vector.Z() << ", " << Vector.W() << ">";
		return Out;
	}

	std::istream& operator >> (std::istream& In, Vector4f& Vector)
	{
		In >> Vector.X() >> Vector.Y() >> Vector.Z() >> Vector.W();
		return In;
	}

	std::ostream& operator << (std::ostream& Out, const Matrix4f& Matrix)
	{
		Out << "Matrix4f" << std::endl;
		Out << Matrix(0, 0) << '\t' << Matrix(0, 1) << '\t' << Matrix(0, 2) << '\t' << Matrix(0, 3) << std::endl;
		Out << Matrix(1, 0) << '\t' << Matrix(1, 1) << '\t' << Matrix(1, 2) << '\t' << Matrix(1, 3) << std::endl;
		Out << Matrix(2, 0) << '\t' << Matrix(2, 1) << '\t' << Matrix(2, 2) << '\t' << Matrix(2, 3) << std::endl;
		Out << Matrix(3, 0) << '\t' << Matrix(3, 1) << '\t' << Matrix(3, 2) << '\t' << Matrix(3, 3) << std::endl;
		return Out;
	}

	std::istream& operator >> (std::istream& In, Matrix4f& Matrix)
	{
		In >> Matrix(0, 0) >> Matrix(0, 1) >> Matrix(0, 2) >> Matrix(0, 3);
		In >> Matrix(1, 0) >> Matrix(1, 1) >> Matrix(1, 2) >> Matrix(1, 3);
		In >> Matrix(2, 0) >> Matrix(2, 1) >> Matrix(2, 2) >> Matrix(2, 3);
		In >> Matrix(3, 0) >> Matrix(3, 1) >> Matrix(3, 2) >> Matrix(3, 3);
		return In;
	}
*/
}